import { ref } from 'vue'
import PreferenceButton from './PreferenceButton'

const recipeCard = {
    props: {
        recipe: Object
    },
    setup() {

    },
    template:  `<div class="recipeCard">

                </div>`
}